{
  "name": "dsx-control-plane-cloudflare",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler d1 execute DB --remote --file=migrations/0001_phase1_nodes.sql --yes && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260831.1",
    "typescript": "^5.9.2",
    "wrangler": "4.127.1"
  }
}
