DSX_CONTROL_PLANE_URL=https://control.example.com
DSX_NODE_NAME=DSX-TEST-01
DSX_HEARTBEAT_SECONDS=30
DSX_REQUEST_TIMEOUT_SECONDS=10
DSX_AGENT_VERSION=0.1.0
# Set DSX_ENROLLMENT_TOKEN only for the one-time `enroll` command, then remove it.
# DSX_ENROLLMENT_TOKEN=dsx_enroll_...
# For a system service, use a root-owned path such as:
# DSX_AGENT_STATE_FILE=/var/lib/dsx-node-agent/identity.json
