# Server-side only. Never prefix these values with NEXT_PUBLIC_.
CONTROL_PLANE_BASE_URL=https://dsx-control-plane.example.com
CONTROL_PLANE_ADMIN_TOKEN=replace-with-server-side-secret

# Temporary operator boundary for the Phase 5 admin UI.
DSX_ADMIN_UI_USER=admin
DSX_ADMIN_UI_PASSWORD=replace-with-a-strong-password
